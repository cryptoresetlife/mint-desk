# 更新说明

## V1.0 · 重点钱包监控

发布标记：`v1.0-wallet-tracker`。界面版本仍为 V1.0。

### 新增

- 独立“重点钱包”页面，支持导入最多 200 个公开地址，设置标签、分类、关联组和重点/观察状态。
- 跟踪 Robinhood 标准 ERC-721 / ERC-1155 活动，区分主动 mint、匹配的 Seaport 买入/卖出、普通转入/转出和自转。
- 重点钱包主动参与提醒，以及默认 5 分钟内至少 3 组钱包参与同一 NFT 合约的信号。已设置为同一关联组的钱包合并计数。
- 使用已配置 HTTP RPC 和可选 WebSocket，保存扫描断点，断线后补查；检查区块变化，历史补查不触发实时提醒。
- 活动名称链接到 OpenSea NFT 页面；可查询系列地板挂单和近期单件成交，也可将合约带入 mint 项目草稿。
- 本地声音提醒、活动类型筛选和标签/地址/合约搜索。

### 发布包与隐私

- 新解压包不含钱包、私钥、API Key、私有 RPC、用户项目、交易与成本记录、监控地址清单、扫描缓存或浏览器数据。
- 公开钱包跟踪无需导入交易钱包；查询行情才需要用户自行配置 OpenSea API Key。
- 保留 MIT 许可证、构建脚本和第三方组件声明。使用源文件白名单打包，排除运行时用户数据。

### 更新方法

先处理正在运行的任务，再从软件内点击“停止任务并退出软件”。完整解压新版，打开 `Mint Desk.exe`，自行填写接口并导入所需公开地址。私钥只在内存中，重启后需重新导入交易钱包。保留旧文件夹作为本机记录备份，不要转发使用过的文件夹。

### 验证与范围

98 项离线测试通过，覆盖原有 mint/上架功能及新增地址校验、转账分类、关联组去重、断点、区块变化和停止处理。另完成 RPC 只读检查及页面操作检查；未发送测试交易。

当前只跟踪 Robinhood 4663 的已支持 NFT 标准。买卖识别限匹配的 Seaport 成交；其他协议或复杂路由可能显示为普通转账。没有自动跟单、全链覆盖、30 天收益回测或钱包身份认证。参与信号不等于利润达标。
